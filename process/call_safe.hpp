#ifndef EXTRACT_PROCESS_CALL_SAFE_HPP_HPP_INCLUDED
#define EXTRACT_PROCESS_CALL_SAFE_HPP_HPP_INCLUDED

#include "call.hpp"

namespace extract
{
namespace process
{
output const
call_safe(
	argument_list const &);
}
}

#endif
