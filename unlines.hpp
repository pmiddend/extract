#ifndef EXTRACT_UNLINES_HPP_HPP_INCLUDED
#define EXTRACT_UNLINES_HPP_HPP_INCLUDED

#include <fcppt/string.hpp>
#include <vector>

namespace extract
{
std::vector<fcppt::string> const
unlines(
	fcppt::string const &);
}

#endif
